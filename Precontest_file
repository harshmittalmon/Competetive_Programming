#include <bits/stdc++.h>
using namespace std;
#define vectinp(v){for(auto&i:v){cin>> i;}}
#define PrintVec(v){for(auto&i:v){cout<< i<<" ";}cout<< endl;}
#define vi vector<int> 
#define vll vector<long long>
#define mapll map<long long,long long>
#define umapll unordered_map<long long ,long long>
#define mapii map<int,int> 
#define u_mapii unordered_map<int,int> 
#define ll long long
#define itc int t; \
            cin>> t;\
            while(t--)
#define all(v) v.begin(),v.end

// -----------string stream usage-------------------------------
string removeSpaces(string str)
{
    // Storing the whole string into string stream
    stringstream ss(str);
    string temp; // temp is for iterating in sstream
 
    // Making the string empty
    str = "";
 
    // Running loop till end of stream and getting every word
    while (getline(ss, temp, ' ')) { // getline( string from which we want input, iterator string, delimiter)
        // Concatenating in the string to be returned
        str = str + temp ;
    }
    return str;
}


void solve(){
    int n;
    cin>>n;
}
int main(){
    itc{
        solve();
    }
}
